# The Voice Design

Artboard. Paint. Press.

The Design room of The Voice — posters, covers, stories, and marks. Type, layers, a real brush engine, command palette, and present mode.

```bash
cd design
npm install
npm run dev
```
