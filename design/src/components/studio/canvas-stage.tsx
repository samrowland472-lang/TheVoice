import { useCallback, useEffect, useRef, useState } from "react";
// Restored - see local workspace. Full file push in progress.
export function CanvasStage() {
  return <div className="pasteboard relative min-h-0 flex-1">Canvas loading…</div>;
}
