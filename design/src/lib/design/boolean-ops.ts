import type { DesignNode } from "./types";

/** Placeholder — full boolean ops (union/subtract) pending polish. */
export function unionShapes(_nodes: DesignNode[]): DesignNode | null {
  return null;
}

export function subtractShapes(_a: DesignNode, _b: DesignNode): DesignNode | null {
  return null;
}
